/**
 * 登录页面 url 地址
 */
const LOGIN_PATH = '/auth/login'

/**
 * 默认首页地址
 */
const DEFAULT_HOME_PATH = '/workspace'

/**
 * 登录 token
 */
const LOGIN_TOKEN = 'login-token'

export { DEFAULT_HOME_PATH, LOGIN_PATH, LOGIN_TOKEN }
