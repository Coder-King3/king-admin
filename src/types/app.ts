/**
 * 主题模式
 * dark 暗黑
 * light 白天
 * auto 自动
 */
type ThemeModeType = 'dark' | 'light' | 'auto'

type ThemeSize = 'default' | 'large' | 'small'

/**
 * 导航风格
 * plain 朴素
 * rounded 圆润
 */
type NavigationStyleType = 'plain' | 'rounded'

export type { NavigationStyleType, ThemeModeType, ThemeSize }
